import { connect } from 'react-redux'
import RunHistory from '../components/RunHistory'

const mapDispatchToProps = {
}

const mapStateToProps = (state) => ({
})

export default connect(mapStateToProps, mapDispatchToProps)(RunHistory)
